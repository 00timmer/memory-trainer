# Memory Trainer 重构设计文档

日期：2026-09-08
基于：用户提出的三系统训练方案（Direct Memory / Fast Encoding / Retrieval Automaticity）
现状：`index.html` 单文件 2260 行，7 种闪卡模式，localStorage 存储

---

# 第 0 部分 — 对训练方案本身的意见

方案整体方向我认同：把「记得住」「编得快」「取得快」拆成三个独立指标，
而不是揉成一个 Memory Score，这是对的。以下 8 点建议修改。

## 0.1 System A 应定位为「测量」，不应期待它训练出容量提升

> **用户 2026-09-08 决定：不采纳本条的「随机序列只做基线」建议，
> 随机序列保留为日常训练科目。仪表盘上会标注「迁移有限」，但不阻止。
> 以下分析保留，作为日后回看数据时的解释框架。**

这是最需要调整的一条。

「一次曝光能记住多少随机材料」这个容量，在认知训练文献里是**最难通过训练迁移的**。
练随机汉字串，会让你在随机汉字串上进步；但这个进步基本**不会迁移**到英语单词、
诗词、歌词这些你真正在意的材料上。这是工作记忆训练领域重复验证最多的负面结果。

所以建议：

- System A 保留，但定位改为 **诊断/基线仪器**，不是训练科目。
  它回答的是「我的瓶颈到底在直接编码，还是在提取」，这个信息本身值钱。
- Part 9 的随机序列 → **只用于标准化基线测试**（Part 14），不做日常训练科目。
- Part 10（有意义材料）和 Part 11（听觉）**优先级提到 A 的最前面**。
  这两个是真的能改善的：句子级的「听一遍留下多少」很大程度是语言熟练度 + 组块能力，
  这个会随练习提升；而且它更接近你实际在意的场景（生词、诗、歌词）。
- 「今日训练」里的 5 分钟 Direct Memory 段，默认给有意义材料，不给随机字符。

对你真正有用的、能提高「第一次就记住」的杠杆，其实是：
**注意力 + 曝光后立刻做一次提取（而不是再看一遍）+ 领域知识积累**。
这些应该做进 A 的训练流程里，而不是练span。

## 0.2 Trials to Acquisition 会被材料难度污染

TTA 要求每次用**全新材料**（否则测的是残留记忆），但这样一来，
本周 TTA=3.2、下周 TTA=4.1，差异更可能来自材料难度而不是能力变化。

修改建议：

- TTA 只在**固定材料类型 + 固定难度参数**内部比较，跨类型不可比。
  App 里分开显示：`TTA(生词)` / `TTA(数字串)` / `TTA(句子)`，不给总的 TTA。
- 单次估计至少 20 个 item 才显示数字，不足时显示「样本不足」。
- 预期改善速度是**月级不是周级**。App 的趋势图默认 90 天窗口，
  7 天窗口上不显示 TTA 趋势线（噪声大于信号）。

## 0.3 提取速度有物理下限，目标值要设渐近线

「Cue 出现 → 按键」这个区间必然包含简单反应时（约 200–250ms）+ 识别 + 运动。
两位数→人物的现实渐近线大约在 **600–900ms**，不是 0.3s。

修改建议：把 Part 17 的分档改成带渐近线的：

| 区间 | 状态 |
|---|---|
| > 3.0s | Struggling 卡壳 |
| 2.0–3.0s | Slow 慢 |
| 1.2–2.0s | Fluent 流畅 |
| 0.8–1.2s | Fast 快 |
| < 0.8s | Automatic 自动 |

到达 Automatic 之后，目标从「更快」改为「**保持 + 降低 P90**」。
P90 比中位数更有意义——中位数 0.8s 但 P90 是 3.5s，说明还有卡壳，只是被平均掉了。

## 0.4 「按键 = 答案浮现」是自我报告，会系统性偏早

你说的流程（按键冻结 → 再看答案 → 自评对错）是对的，比现在好得多。
但它有一个内在漏洞：人在自信时会**提前按**，然后看到答案再判「对」。
这会让延迟曲线好看，而实际能力没变。

修改建议：加**校准试次**。约 1/5 的试次不给自评，而是要求**先产出答案**
（数字系统用选择题 4 选 1，或直接输入），再显示对错。
校准试次的正确率如果明显低于自评试次的正确率，说明自评在放水，
App 应该提示「你的自评正确率比实测高 N%」。

这一条我认为是整个 System C 里最关键的诚实性保障。

## 0.5 延迟测试（24h/3d/7d）在纯网页里没有推送

现在是 GitHub Pages + localStorage，没有后端也没有通知权限。
所以「到点提醒你来测」做不到。

修改建议：

- 延迟测试做成**到期队列**，在你下次打开「今日训练」时**优先插入队首**。
- 实际间隔记录**真实经过时间**（`actualDelayMs`），不记「标称 24h」。
  分析时按真实间隔分桶（12–36h 算作 24h 桶），否则数据不可用。
- 明确接受：延迟测试是机会性的，会有缺口。App 显示「已到期未测：N 项」。

## 0.6 localStorage 不跨设备，必须先做导出/导入

你现在有 APK + 网页两个入口，数据完全分离。再往里堆几个月的训练数据之前，
必须先有 **JSON 导出/导入**，否则清一次浏览器数据就全没了。
这是 Phase 0，先于所有功能。

## 0.7 Part 15 的 7 组编码方法对照实验会做出噪声

7 个条件、每组几个 item，得到的差异几乎肯定是噪声。你会看到「故事法赢了」，
下个月看到「联想法赢了」，两次都不可信。

修改建议：

- 条件砍到 **3 组**：
  - A 直接记忆/复述（无刻意编码）
  - B 轻量编码（组块 / 简单联想 / 语义挂钩）
  - C 结构化编码（固定编码 / 桩 / 宫殿）
- **组内交错**（同一次 session 里三组混着来），不要一天一组——否则测的是当天状态。
- 每组累计 **≥30 item** 才显示对照结论；不足时 App 明确显示「样本不足，暂不给结论」。
- 结论要看**四维**同时呈现：编码时长 / 即时正确率 / 24h 保持 / 学会后的提取延迟。
  只看编码时长会得出错误结论（3 秒编码明天全忘 ≠ 更好）。

## 0.8 听觉模块在 APK 里不工作

Android WebView 不支持 Web Speech API（这是你之前踩过的坑）。
浏览器版没问题，APK 版需要 Capacitor TTS 插件。

修改建议：听觉模块**先只做网页版**，APK 版检测到不支持时把听觉训练置灰并说明原因。
既然你已经选了用 GitHub 链接训练，这个取舍成本很低。

## 0.9 与上一轮代码审查的关系

上一轮审查的第 1、2 组待办（计时口径、取消速度惩罚、F1 强制 3 秒、
方向分离掌握度、nextReview 真正启用）**不再单独做**——它们全部并入 System C 重建。
第 3 组（地点重名、顺序模式拆分）放到最后一期。

---

# 第 1 部分 — 提议的架构

## 1.1 文件结构

现在 2260 行单文件，加三个子系统后会到 6000+ 行，单文件不可维护。
但 APK 打包和本地直接打开又需要单文件。折中方案：**源码分模块，构建产物单文件**。

```
memory-trainer/
├── index.html          ← 构建产物（单文件，内联全部 JS/CSS）。GitHub Pages 和 APK 用这个
├── build.sh            ← 纯 shell 拼接，无 npm 无工具链
├── src/
│   ├── shell.html      ← HTML 骨架 + 各屏幕标记
│   ├── styles.css
│   ├── 00-data.js      ← BUILTIN 编码表（现有，原样搬）
│   ├── 01-store.js     ← localStorage 读写 + 迁移 + 导出/导入
│   ├── 02-decks.js     ← 牌组/标签/自定义编码（现有，原样搬）
│   ├── 03-retrieval.js ← System C：提取自动化
│   ├── 04-direct.js    ← System A：直接记忆
│   ├── 05-encoding.js  ← System B：快速编码
│   ├── 06-materials.js ← 材料生成器（字符池/生词池/数字串/句子池）
│   ├── 07-audio.js     ← 听觉呈现（Web Speech + 降级）
│   ├── 08-scheduler.js ← 到期队列 + 「今日训练」编排
│   ├── 09-dashboard.js ← 三维度仪表盘
│   └── 10-app.js       ← 路由/模态/返回键/启动
└── DESIGN.md
```

`build.sh` 就是 `cat` + 一个 `sed` 把 CSS/JS 插进 shell.html 的占位符。
改完源码跑一次，`index.html` 更新，push 即上线。

## 1.2 三系统的边界

```
                    ┌──────────────── 今日训练（唯一入口）─────────────┐
                    │                                                 │
      ┌─────────────▼──────────┐  ┌───────────▼──────────┐  ┌────────▼─────────┐
      │  A 直接记忆            │  │  B 快速编码          │  │  C 提取自动化    │
      │  新材料 · 禁刻意编码   │  │  新材料 · 任意方法   │  │  已学材料        │
      │  测：一次曝光留存量    │  │  测：建立稳定记忆耗时│  │  测：cue→答案延迟│
      └────────────┬───────────┘  └──────────┬───────────┘  └────────┬─────────┘
                   │                         │                       │
                   └──────────┬──────────────┘                       │
                              ▼                                      │
                    ┌──────────────────┐                             │
                    │  延迟保持队列    │  30s / 5min / 24h / 3d / 7d │
                    │  （A 和 B 共用） │─────────────────────────────┘
                    └──────────────────┘   学会的 item 毕业进入 C
```

关键设计：**B 学会的东西会毕业进入 C**。这是三系统真正连起来的地方——
你用任意方法记住了一批生词，它们之后就进入提取速度训练，直到 Cue→Answer 自动化。
这正好实现 Part 16 的「脱桩」：C 只考 cue→answer，从不要求你复述当初的编码。

---

# 第 2 部分 — 现有功能：可复用的

以下**原样保留，不动**：

| 功能 | 位置 | 说明 |
|---|---|---|
| BUILTIN 编码表（数字/扑克/地点/日语/注音/儿童） | L490–675 | 全部保留 |
| 牌组标签系统 / 切换 / 隐藏 / 恢复 | `renderTabs` L854 等 | 保留 |
| 自定义牌组 CRUD | `createDeck` L926 等 | 保留 |
| 单条编码增删改 | `openEditModal` L1898 等 | 保留 |
| 浏览对照表 | `showBrowse` L1789 | 保留 |
| 易混淆对生成 | `getConfusable` L752 | 保留，接入 C 的混淆弱项 |
| Streak 连续天数 | L1969–2006 | 保留 |
| 模态框系统 / Android 返回拦截 / safe-area | L1936, L2218 | 保留 |
| 训练范围选择器（按组勾选编码） | `renderCodePicker` L1096 | 保留，C 复用 |
| `shuffle` / `pickRandom` / `pickWithRepeat` | L1326–1341 | 保留 |

---

# 第 3 部分 — 现有功能：需要修改的

| 功能 | 现状问题 | 改法 |
|---|---|---|
| `showCurrentCard` / `revealCard` / `judgeCard` L1493–1700 | 计时含翻牌等待+锁定延迟+阅读评分时间 | 重建为「承诺→冻结→核对」三段，`performance.now()` |
| `getFrictionLevel` L1282 | `revealTooFast` 惩罚快速提取；F1 强制等 3 秒 | 删除速度惩罚；F1 取消强制等待，改为「先承诺再看」的流程本身即是阻力 |
| `showJudgeWithSuggestion` L1620 | 用时间建议自评档位，把速度当正确性 | 删除建议高亮；正确性与速度分开记录、分开展示 |
| `f2DontKnow` L1651 | 显示答案后同步跳下一张，没有阅读时间 | 改为「显示答案 → 手动继续 → 隔 3–5 张重测」 |
| `updateCardMastery` L1257 | 3 次「记得」满级；正/反/连读共用等级 | 换成 `mt_retrieval`，按 `code + direction` 分开，四态判定 |
| `nextReview` L1273 | 算了但 `buildDeck` 从不读 | `buildDeck` 接入到期优先 |
| `getDeckMasteryDist` L1303 | 未接触卡片不计入，高估覆盖 | 分母改为全部选中编码，未测单列一档 |
| 弱项系统 L818–830 | 只收「不会」；3 次不同日期成功即毕业 | 拆 Recall Weak / Speed Weak；毕业后保留低频抽查 |
| 冲刺模式 `startSprintTimer` L1740 | 超时只自动翻牌，之后仍可自评「记得」 | 记录 `timedOut` 状态，超时后的正确单独归类 |
| `saveDailyStats` L831 | 只存 correct/wrong/avgTime，且 avg 混了不同模式 | 保留旧表（历史不丢），新数据写入 `mt_retrieval` |
| 首页 L274 | 9 个模式并列，每次要选 | 「今日训练」置顶大按钮；其余模式收进「自定义训练」折叠区 |
| 统计页 `showStatsPage` L1885 | 只有弱项列表 + 7 天流水 | 换成三维度仪表盘 |
| 顺序记忆模式 L2014–2192 | 记忆阶段同时给数字和名称；回忆阶段给全部选项（再认非回忆） | 拆成「编码连读」（测 C）和「宫殿应用」（测 B），最后一期做 |
| 地点表 `LOCATION_DATA` | 125 个地点且重名（21/32=冰箱，38/41/45=床，12/40=书架） | 反向训练前必须消歧，改成「厨房冰箱」等 |

**明确删除**：`revealTooFast` 字段、F1 三秒倒计时、时间建议自评高亮、
`sequence` 模式的全选项回忆界面。

---

# 第 4 部分 — 需要新增的模块

## 4.1 System C — 提取自动化（`03-retrieval.js`）

试次流程：

```
cue 出现  ──t0(performance.now())
   ↓
用户想到答案，按「我想到了」/空格  ──t1  ← 延迟 = t1-t0，此刻冻结，不可再改
   ↓
显示正确答案
   ↓
用户核对：✓对 / △对但模糊 / ✗错          ← 这段时间不计入延迟
   ↓
下一题
```

- 校准试次（约 20%）：t1 之后不显示答案，先要求 4 选 1 或输入，再显示对错。
- 页面切后台的试次打 `bg:true` 标记，不进统计。
- 每个 `code+direction` 保留最近 30 次试次，滚动计算 median / P90 / 正确率。

自动化四态：`未测 → 能正确提取 → 能快速提取 → 隔天仍能快速提取`
（「隔天」= 最近 3 次达标试次落在 3 个不同日期）

## 4.2 自动化地图（`09-dashboard.js` 的一部分）

0–99 网格热力图，每格颜色 = 自动化等级，点开看：

```
37 — 雏田
中位 0.72s · P90 1.01s · 正确率 100% · 最近 12 次
状态 ⚡ Automatic
```

一眼看出瓶颈在哪几个编码。

## 4.3 System A — 直接记忆（`04-direct.js`）

- **有意义材料**（日常主科目）：句子 → 长句 → 两句 → 短段落。
  呈现一次后隐藏，分别评分：大意 / 关键词 / 逐字 / 顺序。四项分开记。
- **随机序列**（仅基线用）：3–7 项自适应，块级调整（每 5 试次一调），
  >90% 升难度，<60% 降难度。
- **标准化基线**（Part 14）：3/4/5/6/7 项 × 各 5 试次，曝光固定 3.0s，
  条件锁死不可调，输出 Direct Memory Capacity（维持 ~75–80% 正确率的最大长度）。
  7 天 / 30 天 / 90 天重跑，只和同条件的历史比。
- 曝光后**立刻做一次提取**（而不是再看一遍），这是 A 里唯一有证据的训练手段。

## 4.4 听觉直接记忆（`07-audio.js`）

- 播放一次，不显示文本，试次内禁止重播。
- 阶梯：5–7 字 → 10 字 → 15 字 → 一句 → 两句 → 20s → 30s。
- 评分维度同上（大意/关键词/逐字/顺序）。
- Web Speech API；不支持时（APK WebView）置灰并提示。

## 4.5 System B — 快速编码（`05-encoding.js`）

```
新材料出现 ──t0
   ↓  （任意方法：复述/组块/联想/图像/故事/桩/宫殿）
按「已记住」 ──t1  ← Encoding Time = t1-t0
   ↓
立即测：即时正确率
   ↓
进入延迟队列：5min / 24h / 3d / 7d
```

- **Effective Encoding Speed** = `Encoding Time / 24h保持率`
  （3 秒编码但明天忘光 → 分母趋 0 → 效率极差。这是方案里最重要的一个复合指标。）
- **方法对照实验**：3 组（直接/轻量/结构化），组内交错，≥30 item 才出结论。
- 学会的 item 毕业 → 写入 `mt_retrieval` 进入 System C 训练。

## 4.6 TTA 测量（`05-encoding.js` 的一部分）

呈现 → 测 → 未达标则再呈现 → 循环至达标，记录曝光次数。
按材料类型分开统计，样本 <20 不显示，趋势只看 90 天窗口。

## 4.7 材料库（`06-materials.js`）

| 材料类型 | 来源 | 防重复 |
|---|---|---|
| 随机汉字 | 内嵌常用字频表（约 2500 字），排除已用 | `usedLedger` |
| 随机数字串 | 生成 | 天然不重复 |
| 生词 | 内嵌低频词表 + 用户可导入自己的词表 | `usedLedger` |
| 句子/段落 | 内嵌句库 + 用户导入 | `usedLedger` |

`usedLedger` 记录每个 item 首次使用时间，保证 System A/B 永远拿到真正的新材料。

## 4.8 到期队列与今日训练（`08-scheduler.js`）

```
打开 App → 「今日训练」→ 自动编排：

  ① 到期的延迟保持测试（最优先，插队首）
  ② 5min  System A  有意义材料 · 新
  ③ 5min  System B  新材料 · 任意方法
  ④ 5-10min System C  按优先级：到期项 > 慢项 > 错项 > 混淆对 > 少量已掌握项抽查
```

三档：**5 分钟速练 / 15 分钟标准 / 20 分钟完整**。
用户不需要选任何东西。原有 9 个模式移到「自定义训练」折叠区，保留但不占首屏。

## 4.9 三维度仪表盘（`09-dashboard.js`）

不给合成总分。三张独立卡片：

```
直接记忆        视觉容量 5.2 · 听觉容量 4.1 · 即时 82% · 24h 61%
编码效率        中位编码 6.4s · TTA(生词) 3.1 · 24h保持 74% · EES 8.6
提取自动化      中位 1.14s · P90 2.31s · 正确率 96% · 自动化 42% · 慢项 11
```

趋势窗口 7 / 30 / 90 天 / 全部。TTA 只在 90 天窗口出趋势线。

## 4.10 导出/导入（`01-store.js`）

一键导出全部数据为 JSON 文件，一键导入合并。**Phase 0 先做**。

---

# 第 5 部分 — 数据结构变更

## 5.1 原则

旧 key **完全不动、不删**，新数据写新 key，迁移只读不写旧表。
任何时候都可以回滚到当前版本而不丢数据。

## 5.2 现有 key（保留）

`mt_deck` `mt_phase` `mt_udecks` `mt_custom2` `mt_weak2` `mt_stats2`
`mt_practiced` `mt_card_mastery` `mt_hidden_decks`

## 5.3 新增 key

```js
mt_retrieval = {
  [deckId]: {
    [code]: {
      fwd: {                      // 也可能是 rev / multi
        trials: [                 // 保留最近 30 次
          { ts, ms, ok, vague, mode, calib, timedOut, bg }
        ],
        median, p90, acc,         // 滚动计算，缓存
        status: 0|1|2|3,          // 未测/能正确/能快速/隔天仍快速
        distinctFastDays: [],     // 达标试次的日期集合
        lastSeen, nextDue
      },
      rev: {...}, multi: {...}
    }
  }
}

mt_weakness = {
  [deckId]: {
    [code]: {
      [dir]: {
        recall: { active, since, clearedDays: [] },   // 想不起/答错
        speed:  { active, since, clearedDays: [] },   // 对但慢
        confuse:{ active, wrongTargets: {} }          // 记录实际误答对象
      }
    }
  }
}

mt_direct = {
  trials: [ { ts, modality:'visual'|'audio', kind:'random'|'meaningful',
              len, exposureMs, scores:{ gist, keyword, verbatim, order },
              itemId } ],
  baselines: [ { ts, byLen:{3:1.0,4:0.96,5:0.79,6:0.57,7:0.31}, capacity:5.2 } ],
  adaptive: { visualLen, audioLen, blockHistory }
}

mt_encoding = {
  items: [ { id, materialType, materialRef, condition:'A'|'B'|'C',
             t0, encodeMs, immediate:{score},
             delayed:[ { label, dueTs, testedTs, actualDelayMs, score } ],
             graduated:false } ],
  tta:  [ { ts, materialType, itemId, exposures, curve:[0.48,0.73,0.91,1.0] } ]
}

mt_queue = [ { itemId, source:'direct'|'encoding', label:'24h',
               dueTs, createdTs } ]

mt_materials_used = { [itemId]: firstUsedTs }

mt_settings = { sessionLen:'15m', calibRate:0.2, audioEnabled:true, ... }

mt_schema_version = 2
```

## 5.4 迁移逻辑（一次性，幂等）

1. 读 `mt_card_mastery[deck][code].level`，写入 `mt_retrieval[deck][code].fwd.status`
   （level 3 → status 1「能正确提取」，不是 2）。
   **理由**：旧数据没有可信延迟（计时口径被污染），不能声称「能快速提取」。
   打 `source:'migrated'`，`trials:[]`。
2. 读 `mt_weak2[deck]`，全部写入 `mt_weakness[deck][code].fwd.recall.active = true`。
3. `mt_stats2` 原样保留，旧的 7 天流水图继续显示，标注「旧口径」。
4. 旧的 `avgTime` **不作为**新延迟基线，仪表盘上不与新数据同图。

---

# 第 6 部分 — 分期实施计划

| 期 | 内容 | 产出 |
|---|---|---|
> **执行状态：P0 / P1 / P2（2026-09-08）、P3（2026-09-10）已完成并上线。**
> P3 按用户决定保留了随机序列作为日常训练科目（0.1 条未采纳），仪表盘与每轮结束页
> 都标注了迁移有限。延迟保持队列已在写入 `mt_queue`，等 P4 消费。

| **P0** | 仓库改结构 + `build.sh` + 导出/导入 JSON + schema 迁移骨架 | 数据安全兜底，功能零变化 |
| **P1** | System C 重建：承诺→冻结→核对计时、删速度惩罚、方向分离、四态、校准试次、慢项/错项拆分、到期抽题 | 提取速度第一次可信可比 |
| **P2** | 「今日训练」编排器 + 三维度仪表盘骨架（先只有 C 有数据）+ 自动化地图 | 一键开练，首屏摩擦降到最低 |
| **P3** | System A：有意义材料 + 曝光后立即提取 + 自适应难度 + 标准化基线 | 直接记忆可测 |
| **P4** | 听觉模块 + 延迟保持队列（30s/5min/24h/3d/7d） | A 的保持曲线成立 |
| **P5** | System B：编码计时 + EES + TTA + 3 组方法对照（带样本量门槛） | 编码效率可测 |
| **P6** | 地点表消歧 + 顺序模式拆成「编码连读」/「宫殿应用」 | 清掉上一轮审查遗留 |

每期结束都构建 `index.html` 并 push，链接立即可用。
P1 结束后建议**先跑一周新口径基线**再进 P2，否则没有对照起点。

---

# 附：不做的事

- 不做合成的「记忆总分」
- 不做把 System A 当训练科目、期待 span 提升的宣传（用户选择保留随机序列训练，
  仪表盘会明确标注迁移有限，但不做「练这个能提升整体记忆力」的暗示）
- 不在样本不足时给出编码方法对照结论
- 不因为答得快而惩罚（<1s 正确就是成功）
- 不要求用户复述当初的编码（脱桩原则）
